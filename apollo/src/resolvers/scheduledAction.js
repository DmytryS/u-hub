import { ScheduledAction } from '../models/index.js'

const query = (parent, args, context, info) => {

}

const mutation = (parent, args, context, info) => {

}

export default {
  query,
  mutation
}