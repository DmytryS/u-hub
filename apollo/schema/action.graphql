type Action {
  id: ID!
  sensor: Sensor!
  valueToChangeOn: Int!
}

input ActionInput {
  id: ID
  sensor: ID!
  valueToChangeOn: Int!
}

# extend type Query {
#   action(input: ActionInput!, sort: String): Action
# }

# extend type Mutation {
#   action(input: ActionInput!, sort: String): Action
# }
