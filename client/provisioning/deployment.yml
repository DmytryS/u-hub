apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
  labels:
    app: client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - name: client
        image: loginkr/u-hub_client:latest
        envFrom:
        - configMapRef:
            name: amqp
        - configMapRef:
            name: mongo
        ports:
        - containerPort: 80
